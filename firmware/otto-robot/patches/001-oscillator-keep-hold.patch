--- a/main/boards/otto-robot/oscillator.cc
+++ b/main/boards/otto-robot/oscillator.cc
@@ -66,8 +66,12 @@ bool Oscillator::NextSample() {
 }
 
 void Oscillator::Attach(int pin, bool rev) {
-    if (is_attached_) {
-        Detach();
+    // Keep holding torque: never ledc_stop + duty 0 when already attached.
+    // Re-Attach after stop used to drop PWM and let servos sag under gravity.
+    if (is_attached_) {
+        pin_ = pin;
+        rev_ = rev;
+        Write(pos_);
+        return;
     }
 
     pin_ = pin;
@@ -94,8 +98,8 @@ void Oscillator::Attach(int pin, bool rev) {
 
     ledc_speed_mode_ = LEDC_LOW_SPEED_MODE;
 
-    // pos_ = 90;
-    // Write(pos_);
+    // Apply hold pulse immediately so duty is never left at 0.
+    Write(pos_);
     previous_servo_command_millis_ = millis();
 
     is_attached_ = true;
